name: dataset-exfiltration
checks:
  - "BigQuery dataset has public/wildcard access"
  - "GCS bucket allows allUsers/allAuthenticatedUsers"
actions:
  - "Revoke wildcard bindings"
  - "Rotate service account keys (if any)"
